#include <stdio.h>
#include <stdlib.h>

int main()
{
    int decimal;
    int  bites[32];
    int i = 0 ;

    printf("donne votre number en dicimal!\n");
    scanf("%d",&decimal);

    if (number == 0) {printf("0");
    else {
        while (number > 0){
            bits[i++] = number % 2 ;
            number = number / 2;
        }
        for (int)
    }





     printf("format hexadécimal est:%x",decimal);



    return 0;
}
